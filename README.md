# RAILFLOW-Dynamic-Railway-ETA-Delay-Propagation-System
A railway operations decision-support system that predicts train ETA, detects resource conflicts, models cascading delay propagation, and enables what-if intervention analysis.
